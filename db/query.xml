<mapper name="BaseMapper">
        <select name="select_test">
                SELECT CUST_NO, EVNT_ID
                FROM TEST
<!--                WHERE CUST_NO = 'CUST0099992'-->
                ORDER BY CUST_NO ASC, ORGN_DTM ASC
<!--                <![CDATA[ WHERE ROWNUM<=1 ]]>-->
        </select>
        <insert name="insert_test">
                INSERT INTO TEST
                 (CUST_NO,
                 ORGN_DTM,
                 EVNT_ID,
                 EVNT_NM,
                 SYS_EVNT_ID,
                 SYS_EVNT_NM)
                VALUES
                 (#{CUSTNO},
                 SYSDATE,
                 #{EVNT_ID},
                 #{EVNT_NM},
                 #{SYS_EVNT_ID},
                 #{SYS_EVNT_NM}
                )
        </insert>
        <insert name="INSERT_TEST_DATA">
                INSERT INTO TEST
                 (CUST_NO,
                 ORGN_DTM,
                 EVNT_ID,
                 EVNT_NM,
                 SYS_EVNT_ID,
                 SYS_EVNT_NM)
                VALUES
                (:1, :2, :3, :4, :5, :6)
        </insert>
</mapper>
# possible to make add, delete, adjust model
# make pipeline api
# can select model(show model list)
# can adjust HP
# can add process(if scripting -> provide pre-built to run (get pp node result, toss to after node run result, kind of wraper) -> save and commit -> apply it to pipeline)
# A. provide copy, B. provide how to doc
# bind mount on same volume (for cluster, at this time)

